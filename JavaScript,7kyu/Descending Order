function descendingOrder(n)
{
 let CN = n.toString();
 let NewMasiv = CN.split('');
 let SortMasiv = NewMasiv.sort();
 let ReverseMasiv = SortMasiv.reverse();
 let joinMasiv = SortMasiv.join('');
 let backNumber = Number(joinMasiv);
 return backNumber;
}
